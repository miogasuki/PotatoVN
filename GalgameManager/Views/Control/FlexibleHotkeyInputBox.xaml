<UserControl
    x:Class="GalgameManager.Views.Control.FlexibleHotkeyInputBox"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Border Background="{ThemeResource TextControlBackground}"
            BorderBrush="{ThemeResource TextControlBorderBrush}"
            BorderThickness="{ThemeResource TextControlBorderThemeThickness}"
            CornerRadius="{ThemeResource ControlCornerRadius}"
            MinHeight="32"
            Padding="11,5,11,6">
        <Grid>
            <!-- 占位符文本 -->
            <TextBlock x:Name="PlaceholderTextBlock"
                       Text="{x:Bind PlaceholderText, Mode=OneWay}"
                       Foreground="{ThemeResource TextControlPlaceholderForeground}"
                       Visibility="{x:Bind ShowPlaceholder, Mode=OneWay}"
                       VerticalAlignment="Center"
                       IsHitTestVisible="False" />

            <!-- 显示按键的文本和清除按钮 -->
            <Grid x:Name="DisplayGrid"
                  Visibility="{x:Bind HideText, Mode=OneWay}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="HotkeyDisplayTextBlock"
                           Grid.Column="0"
                           Text="{x:Bind HotkeyDisplayText, Mode=OneWay}"
                           Foreground="{ThemeResource TextControlForeground}"
                           VerticalAlignment="Center"
                           IsHitTestVisible="False" />

                <Button x:Name="ClearButton"
                        Grid.Column="1"
                        Background="Transparent"
                        BorderThickness="0"
                        Padding="5,0"
                        Margin="5,0,0,0"
                        Click="ClearButton_Click"
                        ToolTipService.ToolTip="清除快捷键">
                    <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE74D;" FontSize="12" Foreground="{ThemeResource TextControlForeground}" />
                </Button>
            </Grid>

            <!-- 输入区域 - 支持键盘和鼠标输入 -->
            <Border x:Name="InputBorder"
                    Background="Transparent"
                    BorderThickness="0"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    Padding="0"
                    Visibility="{x:Bind ShowInputButton, Mode=OneWay}"
                    Tapped="InputBorder_Tapped"
                    PointerEntered="InputBorder_PointerEntered"
                    PointerExited="InputBorder_PointerExited"
                    PointerPressed="InputBorder_PointerPressed">
                <TextBlock x:Name="InputTextBlock"
                           Text="{x:Bind ButtonText, Mode=OneWay}"
                           Foreground="{ThemeResource TextControlForeground}"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Left"
                           Margin="0" />
            </Border>
        </Grid>
    </Border>
</UserControl>