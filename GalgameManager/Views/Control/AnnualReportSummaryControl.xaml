<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="GalgameManager.Views.Control.AnnualReportSummaryControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:GalgameManager.ViewModels"
    xmlns:converter="using:GalgameManager.Helpers.Converter"
    mc:Ignorable="d">

    <UserControl.Resources>
        <converter:ImagePathConverter x:Key="ImagePathConverter" />
    </UserControl.Resources>

    <Grid Width="400" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="PotatoVN 年度报告" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>
        
        <TextBlock Grid.Row="1" Text="{x:Bind viewModels:AnnualReportData.Year}" FontSize="18" HorizontalAlignment="Center" Margin="0,0,0,10" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
            <StackPanel Margin="10">
                <TextBlock Text="总游玩时长" Foreground="{ThemeResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center"/>
                <TextBlock Text="{x:Bind Data.PlayedTime, Mode=OneWay}" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center"/>
                <TextBlock Text="小时" FontSize="12" HorizontalAlignment="Center"/>
            </StackPanel>
            <StackPanel Margin="10">
                <TextBlock Text="游玩游戏数" Foreground="{ThemeResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center"/>
                <TextBlock Text="{x:Bind Data.TotalGamesPlayed, Mode=OneWay}" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center"/>
                <TextBlock Text="部" FontSize="12" HorizontalAlignment="Center"/>
            </StackPanel>
        </StackPanel>

        <Grid Grid.Row="3" Margin="0,20" CornerRadius="8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" Padding="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Image Source="{x:Bind Data.FavoriteGame.ImagePath, Converter={StaticResource ImagePathConverter}, Mode=OneWay}" 
                   Width="80" Height="110" Stretch="UniformToFill" Margin="0,0,10,0"/>
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock Text="年度最爱" Foreground="{ThemeResource SystemControlForegroundAccentBrush}" FontWeight="Bold"/>
                <TextBlock Text="{x:Bind Data.FavoriteGame.Name.Value, Mode=OneWay}" TextWrapping="Wrap" FontWeight="SemiBold" Margin="0,5"/>
                <TextBlock>
                    <Run Text="贡献了"/>
                    <Run Text="{x:Bind Data.FavoriteGamePlayedTime, Mode=OneWay}" FontWeight="Bold"/>
                    <Run Text="小时"/>
                </TextBlock>
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="4" Spacing="10" Margin="0,10">
            <Grid>
                <TextBlock Text="最长连续游玩" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock Text="{x:Bind Data.LongestStreak, Mode=OneWay}" FontWeight="Bold" Foreground="{ThemeResource SystemControlForegroundAccentBrush}"/>
                    <TextBlock Text=" 天" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
            <Grid>
                <TextBlock Text="年度入库" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock Text="{x:Bind Data.NewGamesCount, Mode=OneWay}" FontWeight="Bold" Foreground="{ThemeResource SystemControlForegroundAccentBrush}"/>
                    <TextBlock Text=" 部" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
            <Grid>
                <TextBlock Text="平均评分" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock Text="{x:Bind Data.AverageRating, Mode=OneWay}" FontWeight="Bold" Foreground="{ThemeResource SystemControlForegroundAccentBrush}"/>
                    <TextBlock Text=" 分" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
            <Grid>
                <TextBlock Text="吐槽字数" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock Text="{x:Bind Data.CommentWordCount, Mode=OneWay}" FontWeight="Bold" Foreground="{ThemeResource SystemControlForegroundAccentBrush}"/>
                    <TextBlock Text=" 字" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </StackPanel>

        <TextBlock Grid.Row="8" Text="Generated by PotatoVN" FontSize="10" Foreground="{ThemeResource TextFillColorTertiaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,20,0,0"/>
    </Grid>
</UserControl>
